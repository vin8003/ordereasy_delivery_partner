# ordereasy_delivery_partner
OrderEasy delivery partner Flutter app (greenfield)

Shared Dart packages live in `packages/`:

- `oe_pod_stub` — dummy proof-of-delivery photo stub (in-memory OFD store)
