# Changelog

## 0.1.0

- Add `MapStop` destination model (`lat`, `lng`, `label`).
- Add `MapLauncherStub` URL builder for `geo:` and OpenStreetMap links.
- Add optional `MapStubPlaceholder` widget (coordinates + Open in maps).
