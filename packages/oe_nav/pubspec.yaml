name: oe_nav
description: >
  Isolated bottom navigation and route-shell stub for the OrderEasy
  delivery-partner app. Dummy placeholders only — never a live host.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
